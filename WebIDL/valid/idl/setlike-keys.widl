interface KeysA {
  readonly setlike<DOMString>;
  static void keys();
};

interface KeysB {
  readonly setlike<DOMString>;
  static readonly attribute long keys;
};

interface KeysE : ReadOnly {
  void keys();
};

interface KeysF : ReadOnly {
  readonly attribute long keys;
};

interface KeysG : ReadOnly {
  const long keys = 0;
};
