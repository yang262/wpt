interface ForEachA {
  readonly setlike<DOMString>;
  static void forEach();
};

interface ForEachB {
  readonly setlike<DOMString>;
  static readonly attribute long forEach;
};

interface ForEachE : ReadOnly {
  void forEach();
};

interface ForEachF : ReadOnly {
  readonly attribute long forEach;
};

interface ForEachG : ReadOnly {
  const long forEach = 0;
};
